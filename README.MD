# Parking System

A Django-based parking management system for managing vehicles, parking spots, and parking sessions.

## Tech Stack

- Python (Django)
- PostgreSQL
- Django ORM
- Environment variables with python-decouple

## Setup (Development)

1. Create and activate virtualenv
2. Install dependencies:
```bash
   pip install -r requirements.txt